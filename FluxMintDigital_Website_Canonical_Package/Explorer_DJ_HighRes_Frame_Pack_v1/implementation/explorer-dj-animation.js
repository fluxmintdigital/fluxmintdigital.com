export async function playFrames(imgEl, urls, {fps=8, loop=false, pingPong=false}={}){await Promise.all(urls.map(u=>new Promise((r,j)=>{const i=new Image();i.onload=r;i.onerror=j;i.src=u;})));if(matchMedia('(prefers-reduced-motion: reduce)').matches||urls.length<2){imgEl.src=urls[0];return;}const f=pingPong?[...urls,...urls.slice(1,-1).reverse()]:urls;let n=0;return new Promise(r=>{const id=setInterval(()=>{imgEl.src=f[n++];if(n>=f.length){if(loop)n=0;else{clearInterval(id);r();}}},1000/fps);});}
